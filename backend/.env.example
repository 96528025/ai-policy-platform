# Copy this file to `.env` and fill in your own values.
# 复制本文件为 `.env` 并填入你自己的值。

# Optional: used when the AI drafting feature is wired up to an LLM.
# 可选:接入 LLM 的 AI 策略草案功能时使用。
# OPENAI_API_KEY=your-key-here
